# DESIGN.md

## 目的

このドキュメントは、大会用サイト全体のデザイン方針を定義する。
対象はトップページ、大会ルール、キラー制限、MAP、ポイント制、アンケート導線、対戦表などの画面である。

この文書はデザイン参考資料や個別の実装指示の全文集約先ではなく、全体方針と参照索引として維持する。

## デザイン資料の参照方針

詳細な役割、比較結果、矛盾時の扱いは`docs/ui/design-references.md`で管理する。

| 資料 | 主な参照用途 | 優先する判断 |
|---|---|---|
| `docs/DESIGN-claude.md` | フォント、余白、雰囲気、世界観、画面全体の印象、文体トーン | フォント、雰囲気、質感、演出方向 |
| `docs/DESIGN-cohere.md` | 色遣い、配色、カラートークン、背景色、強調色、警告色、状態色 | 色に関する判断 |
| `docs/ui/getdesign-implementation-instructions.md` | 今後取得するgetdesign由来の画面別・実装別指示 | 記録された対象画面と適用範囲内 |
| `docs/ui/image-card-guidelines.md` | キラー、MAP、アドオンなどの画像カード、レアリティ背景、選択・禁止状態 | 画像付きカードの共通表示 |

- 色に関する判断では`docs/DESIGN-cohere.md`を優先する。
- フォント、雰囲気、質感、演出方向では`docs/DESIGN-claude.md`を優先する。
- 参考資料はそのまま採用する仕様書ではなく、本サイトの目的、アクセシビリティ、競技情報の明瞭さを満たす範囲で参照する。
- 資料間または既存docsとの矛盾がある場合は、値や表現を混合して実装せず、矛盾点を整理してユーザへ確認する。
- getdesign由来の具体的な指示はこの文書へ全文転載せず、専用文書へ記録する。
- getdesign指示を実装へ反映した場合は、変更履歴または該当するdocsへ反映する。

## デザイン原則

### 1. 参加者が迷わないこと

大会参加者が、必要な情報へ短時間で到達できることを最優先する。
装飾よりも、ルール、禁止事項、未定事項、変更点の見つけやすさを優先する。

### 2. 確定事項と検討中事項を分けること

ルールや制限は、以下の状態を視覚的に区別する。

- 確定
- 検討中
- 変更予定
- 使用禁止
- 条件付き使用可

### 3. スマホ閲覧を前提にすること

参加者はスマホで閲覧する可能性が高いため、スマホ表示を優先する。
PC表示は情報量を増やしてよいが、スマホで読めないレイアウトは禁止する。

### 4. 競技サイトとして過度な演出を避けること

動きの強いアニメーション、読みにくい背景、過度な装飾は避ける。
視認性と操作性を優先する。

## 基本トーン

- 暗色ベース
- 高コントラスト
- 重要事項は強調
- 注意事項は警告色または枠で明示
- 長文ルールは見出しとカードで分割

## 推奨レイアウト

### トップページ

- 大会名
- 開催日時
- 最新のお知らせ
- 主要ページへのリンク
- 変更履歴への導線

### 大会ルールページ

- 基本情報
- 試合形式
- 勝敗判定
- サバイバールール
- キラールール
- MAPルール
- 禁止事項
- 検討中事項

### キラー制限ページ

- キラー名
- 使用可否
- パーク制限
- アドオン制限
- MAP制限
- フックボーナス
- 補足・注意事項

### MAPページ

- 室内MAP
- 屋外MAP
- 使用可能MAP
- 条件付きMAP
- キラー別制限との関係

## コンポーネント方針

React移行後も、コンポーネントは見た目だけでルールの意味を決定しない。
表示内容は承認済みの公開データから受け取り、状態と公開可否を一貫して表現する。

### カード

キラー、MAP、ルール項目など、一覧性が必要な情報はカード化する。
カードには以下を含める。

- タイトル
- 状態ラベル
- 主要制限
- 詳細リンク

### ラベル

状態を示すラベルを使用する。

| ラベル | 用途 |
|---|---|
| 使用禁止 | 使用できない要素 |
| 条件付き | 条件を満たす場合のみ使用可能 |
| 検討中 | まだ確定していない内容 |
| 確定 | 確定済みの内容 |
| 変更予定 | 今後変更される可能性が高い内容 |

### Reactコンポーネントでの状態表示

- `status`は内容の確定状態を表し、`isPublished`は公開可否を表す。
- `isPublished`が`false`の項目は画面へ表示しない。
- `status`が検討中または未定の項目を公開する場合は、本文だけでなく状態ラベルを併記する。
- 使用可否と検討状態は別の概念として扱う。例えば「使用禁止案を検討中」は、検討中の状態と使用禁止の内容を分けて表示する。
- 状態は色だけに依存せず、ラベル文字と必要な補足文を表示する。
- 状態ラベルの文言と意味は全ページで統一する。

### 初期共通コンポーネント

React移行の最小実装では、以下を共通コンポーネント候補とする。

- `PageLayout`
- `SiteHeader`
- `SiteNavigation`
- `SiteFooter`
- `ContentCard`
- `StatusBadge`
- `NoticeBox`
- `RuleSection`

コンポーネントの詳細仕様は`docs/ui/component-guidelines.md`で管理する。

### 初期ページ

初期実装の対象は以下とする。

- `HomePage`
- `RulesPage`
- `NotFoundPage`

キラー、MAP、ポイント、変更履歴の専用ページは、公開データとルールの承認後に追加する。

### 表

ポイント表、禁止パーク一覧、MAPプールなどは表を使用してよい。
スマホでは横スクロールまたはカード表示に切り替える。

## レスポンシブ方針

- スマホでは1カラムを基本とする。
- PCでは2〜3カラムのカード表示を許可する。
- 横幅が狭い場合、ナビゲーションは縦並びにする。
- 表が読みにくい場合はカード化を検討する。

## アクセシビリティ方針

- 文字サイズは小さくしすぎない。
- 色だけで状態を判別させない。
- リンクテキストはリンク先が分かる文言にする。
- 背景画像の上に本文を直接置かない。
- コントラスト不足の配色は避ける。

## 文言方針

- 参加者向けの文言は簡潔にする。
- 運営内検討メモと公開ルール文言を混在させない。
- 「検討中」は必ず検討中として表示する。
- 禁止事項は曖昧にしない。

## 画像・アイコン方針

キラーやMAPのリンクに画像を使う場合は、以下を守る。

- 画像だけで意味を伝えず、テキストも併記する。
- 著作権や利用条件を確認する。
- スマホでタップしやすいサイズにする。
- 画像読み込みに失敗しても意味が伝わるようにする。

キラー、MAP、アドオンなどの画像カードを表示する場合は、`docs/ui/image-card-guidelines.md`を共通仕様として適用する。
画像とデータ上の日本語表示名を常にセットで扱い、選択中、選択不可、使用禁止などの状態を色だけで伝えない。
アドオンのレアリティ背景と内部IDの定義も同文書を正本とする。

## 変更時のルール

デザイン変更を行う場合は、以下を確認する。

1. `DESIGN.md` に反していないか
2. `docs/ui/design-references.md`と対象の参考資料を確認したか
3. getdesign指示の対象である場合、専用文書の適用範囲と未適用項目を確認したか
4. `docs/README.md` に関連ドキュメントがあるか
5. ルール文言の意味が変わっていないか
6. スマホ表示が崩れていないか
7. PC表示で情報が探しにくくなっていないか

## 未決事項

以下は今後決める。

- 正式な配色
- フォント
- キラー画像の正式な使用範囲と権利確認
- MAP画像の正式な使用範囲と権利確認
- アドオンのレアリティ背景色の正式な色値
- スマホ用ナビゲーション形式
- 変更履歴の表示方式
